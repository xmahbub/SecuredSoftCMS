## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
```
Then, build the development server:

```bash
npm run buid
# or
yarn buid
```

Start, The build version:

```bash
npm run start
# or
yarn start
```
